# st_ecs
Single Table Entity Component System, This is an ECS implementation that stores all possible components for each entity. The system contains only one table that holds everything. The user can enable or disable individual components. Queries are performed by iterating over bitmaps representing component activity.
